#!/usr/bin/make -f

%:
	dh $@
	git config --global --add safe.directory $(PWD)

override_dh_auto_build:
	cd demo && cmake -Bbuild
	cd demo/build && cmake --build .

override_dh_auto_clean:

override_dh_auto_install: